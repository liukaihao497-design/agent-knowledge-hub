# V3 验收记录

## 知识库管理升级验收

新增正式知识库列表、创建、名称/说明编辑、停用与启用，替换原来手工输入 `demo` 的方式。后端、迁移及隔离机制见 [知识库 V2 说明](../knowledge-v2/README.md)。以下与后文的初始前端验收分开记录。

- Java：`docs/knowledge-v2/run-tests.ps1 -Offline`，39 项测试通过；包含双向知识库隔离、同名同内容独立上传、错误索引来源过滤、资源归属与状态约束。
- V3 代理：`node --test docs/agent-chat-v3/tests/verify.mjs`，11 项检查通过；增加知识库生命周期接口转发，以及缺失/错误知识库参数下的资源读取验证。
- 浏览器模拟环境：已验证创建后自动选中、空库提问、编辑名称、停用后原会话保留且发送/上传禁用、重新启用、切库开始新会话。
- 引用抽屉：原文件链接携带回答对应的 `knowledgeBaseId=demo`；切到学习笔记后，项目资料的回答和引用不进入新会话。
- 管理页上传：当对话选学习笔记、管理页选项目资料时，侧栏上传窗口明确显示上传到项目资料。
- 桌面管理布局已查看；V2 四个原始文件的字节保护检查通过。旧测试页仅补充资源请求的知识库参数，未重新设计。

本轮使用本地模拟后端验证页面与代理，Java 测试使用模拟外部依赖。2026-09-07 已完成本机真实 MySQL 迁移，原有 7 份文档、114 个分段的表 CHECKSUM 保持一致，`demo` 已登记为 ACTIVE；迁移前已备份知识库两张原表。真实 PgVector、MinIO 和模型的完整调用尚未联调。

## 初始前端版本验收（管理升级前）

日期：2026-09-07。

## 文件范围

- 新增内容均位于 `docs/agent-chat-v3/`。
- `v2-baseline.json` 保存复制前 V2 四个文件的 SHA-256；自动检查确认它们仍逐字节相同。
- 本次未修改 Java、数据库结构、现有知识库页面及 V2 历史存储。

## 浏览器验证

在本地模拟后端 `127.0.0.1:18099` 和独立代理页面 `127.0.0.1:3104` 上验证，模拟数据与真实服务隔离。

| 场景 | 结果 |
| --- | --- |
| 初次打开、重新打开默认知识库问答 | 通过；原会话仍保存在侧栏 |
| 知识库提问、Markdown、引用卡片、正文引用按钮 | 通过 |
| 来源抽屉、原文件链接 | 通过；片段内容按纯文本展示 |
| HTML/script 片段及 javascript 链接 | 通过；脚本未执行，危险链接被净化 |
| 切换 Agent 3，设置最大步数为 2 | 通过；模拟后端收到对应配置，最终回答正确 |
| 分段 SSE，包括跨字节边界的中文数据 | 通过；分析、执行、监督与最终回答完整展示 |
| 切换 Agent 4 | 通过；开启独立任务会话 |
| SSE 未返回最终答案就结束 | 通过；保留轨迹并明确提示请求未完成 |
| 历史会话恢复、轨迹折叠 | 通过 |
| 文档列表、失败状态、第二页边界 | 通过 |
| 文档片段及加载更多（共 12 个） | 通过 |
| 文件选择及上传 `tests/sample.md` | 通过；multipart 请求成功，显示 READY 和开始提问入口 |
| 空知识库、空召回、模型错误 | 通过；空态和错误信息明确展示 |
| 管理页“基于此知识库提问” | 通过；对应知识库进入新的问答会话 |
| 390 × 844 手机布局 | 通过；导航、会话、来源抽屉可用，页面无横向溢出 |
| 后端离线 | 通过；真实 8099 端口未运行时展示连接失败与重试入口 |

修复了验收时发现的顶部状态残留：切换到新会话或知识库管理页，不再继承上一轮任务的失败提示。

## 自动检查

```powershell
node --check docs/agent-chat-v3/app.js
node --check docs/agent-chat-v3/serve.mjs
node --test docs/agent-chat-v3/tests/verify.mjs
```

结果：语法检查通过，9 项测试全部通过。覆盖 V2 字节保护、独立静态资源、资料分页和空范围、问答请求及引用、错误状态传递、multipart 上传、SSE 流式转发、原文件重定向以及代理访问范围。

## 尚未验证的真实环境部分

验收时 Java 后端的 8099 端口未启动，因此没有执行真实模型回答、MinIO 上传和数据库写入。模拟接口验证的是前端交互及接口契约；真实服务联调仍需启动后端与知识库依赖，操作方式见 README。
